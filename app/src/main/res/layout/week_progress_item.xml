<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="prog"
            type="data.model.WeekProgressItem" />
    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        xmlns:tools="http://schemas.android.com/tools">

        <ProgressBar
            android:id="@+id/monProgress"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="30dp"
            android:layout_height="100dp"
            android:layout_marginBottom="25dp"
            android:layout_gravity="center_horizontal"
            android:progress="@{prog.progress}"
            android:max="100"
            android:progressDrawable="@drawable/vertical_progress_drawable" />

        <TextView
            android:id="@+id/percentageTextView"
            android:layout_width="30dp"
            android:layout_height="wrap_content"
            tools:text="23%"
            android:layout_gravity="top|center_horizontal"
            android:textSize="14sp"
            app:percentText="@{prog.progress}"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:layout_marginTop="5dp"
            android:gravity="center"/>

        <TextView
            android:id="@+id/dayOfWeekTextView"
            android:layout_width="37dp"
            android:layout_height="20dp"
            android:layout_gravity="bottom|center_horizontal"
            android:gravity="center"
            android:text="@{prog.dayOfWeek}"
            android:textSize="14sp"
            tools:text="MON"/>

    </FrameLayout>
</layout>